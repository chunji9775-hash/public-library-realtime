{
  "name": "public-library-realtime",
  "version": "1.0.0",
  "description": "Public Library Real-time Status",
  "scripts": {
    "build": "mkdir -p dist && cp index.html styles.css app.js dist/"
  }
}